# abp-api-versioning

Standalone API version model and negotiation helpers extracted from `abp-daemon`.

This crate provides:
- `ApiVersion` parsing/comparison
- `ApiVersionRegistry` endpoint support tracking
- `VersionNegotiator` compatibility negotiation
