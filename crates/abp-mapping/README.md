# abp-mapping

Cross-dialect mapping validation for the Agent Backplane.

Provides `MappingRule`, `MappingRegistry`, `MappingMatrix`, and validation
helpers to verify feature fidelity when translating between agent dialects
(OpenAI, Claude, Gemini, Codex, Kimi, Copilot).
