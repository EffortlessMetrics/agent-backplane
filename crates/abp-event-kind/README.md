# abp-event-kind

Canonical naming helpers for `abp_core::AgentEventKind` variants.

This microcrate centralizes the snake_case event-kind labels used across stream
and sidecar middleware components.
