# abp-rule-engine

A focused rule engine crate for evaluating composable match conditions against
resources and selecting the highest-priority effect.

This crate is intentionally narrow in scope and reusable outside of `abp-policy`.
