# abp-run-queue

A small single-responsibility crate providing a bounded, priority-aware FIFO run queue.

It is intentionally transport/runtime agnostic so it can be reused by daemon and other orchestration surfaces.
