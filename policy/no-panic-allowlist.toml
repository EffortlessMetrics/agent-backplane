schema_version = "0.3"

# Panic-family exceptions use semantic identity: path + family + selector.
# last_seen line/column fields are advisory repair hints only. This first
# policy PR creates the standard file; follow-up remediation PRs should migrate
# any remaining panic-family call sites into explicit entries or remove them.
